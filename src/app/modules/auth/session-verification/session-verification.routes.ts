import { Routes } from '@angular/router';
import { SessionVerificationComponent } from './session-verification.component';

export default [
    {
        path: '',
        component: SessionVerificationComponent,
    },
] as Routes;
